services:
  postgres:
    image: postgres:16-alpine
    container_name: budgetbrain-db
    environment:
      POSTGRES_USER: budgetbrain
      POSTGRES_PASSWORD: budgetbrain
      POSTGRES_DB: budgetbrain
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U budgetbrain']
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: .
    container_name: budgetbrain-api
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: development
      PORT: 3000
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: budgetbrain
      DB_USER: budgetbrain
      DB_PASSWORD: budgetbrain
      JWT_ACCESS_SECRET: dev-access-secret-minimum-32-characters-long
      JWT_REFRESH_SECRET: dev-refresh-secret-minimum-32-characters-long
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - uploads_data:/app/uploads

volumes:
  postgres_data:
  uploads_data:
